# DevDesk

Personal multi-project workspace (`docker-core`, `ecc`, `invoice-generator`, `kinventory`, …).

Cursor efficiency config lives in `~/.cursor/` (rules + hooks). Project ignores: `.cursorignore`, `.cursorindexingignore`.

See `~/.cursor/efficiency-model-policy.md` for model / Max Mode habits.
